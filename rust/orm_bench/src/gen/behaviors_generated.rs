#![allow(non_snake_case, unused_imports, dead_code, clippy::all)]
#![allow(dead_code, unused_imports, non_snake_case)]
// GENERATED by behavior-contracts (rust-typed-native — the RUNTIME-FREE read de-box; STRUCT-ONLY).
//
// HAND-AUTHORED stand-in for post-#164/#165 bc regen — will be replaced by regen. This file is the
// EXACT final form the `rust-typed-native` emitter produces once bc#164 (wire-passthrough output) and
// bc#165 (`--shared-types-import`) land: a covered read is a componentRef chain emitted as a
// STRUCT-returning runner that calls the op-agnostic leaf TRANSPORT symbol DIRECTLY at each covered
// node's execution point (`execute_sql` / `pluck_keys` / `group_children`). Under #164 an INTERMEDIATE
// node (parent executeSQL / pluck / child executeSQL / non-terminal group) holds its result as RAW
// wire (`Vec<WireValue>`, the list ITEMS) and is NEVER de-boxed — only the TERMINAL node de-boxes
// INLINE (fully-unrolled probe/match — no decode helper) into its concrete outType struct. The leaves
// take/return `&[WireValue]` (the wire crosses ONLY at execute_sql params and the pluck/group leaf
// boundary — no key-value container / box is built on the covered plane). Under #165 the BC-owned wire
// + error-value types (`WireValue`/`Probe`/`NumProbe`/`BehaviorError`/`ErrorDetail`/`ErrorKind`) are
// emitted into the SHARED import (`litedbmodel_runtime`), so the leaf that BUILDS them and this runner
// that de-boxes them reference the SAME concrete types. The exposed API is
// run_native_raw_struct_<comp>(in_) over the CONCRETE InNR_<comp> input struct; the driver is resolved
// from the ambient scope (`litedbmodel_runtime::with_ambient_driver`) the consumer brackets the call with.
use std::cell::RefCell;

// op-agnostic leaf transport symbols + the BC-owned shared wire/error types (#164/#165 shared import).
use litedbmodel_runtime::{
    execute_sql, group_children, pluck_keys, BehaviorError, ErrorDetail, ErrorKind, NumProbe, Probe,
    Value, WireList, WireRow, WireValue,
};

// op_failed — wrap a leaf failure as the node's Failure under its Error Policy Kind, carrying the
// leaf's structured detail through unchanged (the runner transports the Error Value; it does not
// synthesize or re-interpret it).
#[allow(dead_code)]
fn op_failed(node: &str, policy: &str, e: BehaviorError) -> BehaviorError {
    BehaviorError {
        code: "OP_FAILED".to_string(),
        message: format!("operation '{node}' failed under '{policy}' policy: {}", e.message),
        detail: e.detail,
    }
}

// de_type_mismatch / de_missing_field / de_overflow — the de-box mismatch failures the generated decode
// produces (scp-error.md "The Error Value"). Codes are byte-equal to the runtime outType check
// (TYPE_MISMATCH / MISSING_PROP) so the covered read stays equivalent to run_behavior; detail.kind
// distinguishes typeMismatch / missingField / overflow.
#[allow(dead_code)]
fn de_type_mismatch(model: &str, field: &str, expected: &str, actual_wire: String, raw: String) -> BehaviorError {
    BehaviorError::with_detail(
        "TYPE_MISMATCH",
        format!("node '{model}': {field}: expected {expected}, got {actual_wire}"),
        ErrorDetail {
            kind: Some(ErrorKind::TypeMismatch),
            model: Some(model.to_string()),
            field: Some(field.to_string()),
            expected_type: Some(expected.to_string()),
            actual_wire_type: Some(actual_wire),
            raw_value: Some(raw),
            context: std::collections::BTreeMap::new(),
        },
    )
}

#[allow(dead_code)]
fn de_missing_field(model: &str, field: &str, expected: &str) -> BehaviorError {
    BehaviorError::with_detail(
        "MISSING_PROP",
        format!("node '{model}': {field}: required field is absent"),
        ErrorDetail {
            kind: Some(ErrorKind::MissingField),
            model: Some(model.to_string()),
            field: Some(field.to_string()),
            expected_type: Some(expected.to_string()),
            actual_wire_type: None,
            raw_value: None,
            context: std::collections::BTreeMap::new(),
        },
    )
}

#[allow(dead_code)]
fn de_overflow(model: &str, field: &str, expected: &str, actual_wire: String, raw: String) -> BehaviorError {
    let message = format!("node '{model}': {field}: {raw} is outside the range of {expected}");
    BehaviorError::with_detail(
        "TYPE_MISMATCH",
        message,
        ErrorDetail {
            kind: Some(ErrorKind::Overflow),
            model: Some(model.to_string()),
            field: Some(field.to_string()),
            expected_type: Some(expected.to_string()),
            actual_wire_type: Some(actual_wire),
            raw_value: Some(raw),
            context: std::collections::BTreeMap::new(),
        },
    )
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct T0 {
    pub id: Option<f64>, // "id"
    pub email: Option<String>, // "email"
    pub name: Option<String>, // "name"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct T1 {
    pub id: Option<f64>, // "id"
    pub title: Option<String>, // "title"
    pub content: Option<String>, // "content"
    pub published: Option<f64>, // "published"
    pub author_id: Option<f64>, // "author_id"
    pub created_at: Option<String>, // "created_at"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct T2 {
    pub id: Option<f64>, // "id"
    pub title: Option<String>, // "title"
    pub author_id: Option<f64>, // "author_id"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct T3 {

}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct T4 {
    pub id: Option<f64>, // "id"
    pub body: Option<String>, // "body"
    pub post_id: Option<f64>, // "post_id"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct T5 {
    pub tenant_id: Option<f64>, // "tenant_id"
    pub user_id: Option<f64>, // "user_id"
    pub name: Option<String>, // "name"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct T6 {
    pub tenant_id: Option<f64>, // "tenant_id"
    pub post_id: Option<f64>, // "post_id"
    pub user_id: Option<f64>, // "user_id"
    pub title: Option<String>, // "title"
}

#[allow(dead_code)]
fn unknown_component(component: &str) -> BehaviorError {
    BehaviorError::new("UNKNOWN_COMPONENT", format!("component '{component}' has no handler (fail-closed)"))
}

// leaf_failure — a failure the LEAF reports (test glue): the runner assigns the node's failure code
// from its Error Policy Kind, so what a leaf carries is its message and its structured Error Value.
#[allow(dead_code)]
fn leaf_failure(message: impl Into<String>) -> BehaviorError {
    BehaviorError::new("LEAF_FAILURE", message)
}

// Native ports structs (one per componentRef node; typed per the static port type — CONCRETE).
// PortsNRFindAllN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRFindAllN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRFilterPaginateSortN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRFilterPaginateSortN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRFindFirstN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRFindFirstN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRFindUniqueN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRFindUniqueN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedFindAllN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindAllN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedFindAllN1 — CONCRETE native ports for node 'n1' (pluck). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindAllN1 {
    pub f_col: String, // "col"
    pub f_rows: Vec<WireValue>, // "rows"
}

// PortsNRNestedFindAllN2 — CONCRETE native ports for node 'n2' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindAllN2 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedFindAllN3 — CONCRETE native ports for node 'n3' (group). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindAllN3 {
    pub f_children: Vec<WireValue>, // "children"
    pub f_fk: String, // "fk"
    pub f_into: String, // "into"
    pub f_parents: Vec<WireValue>, // "parents"
    pub f_pk: String, // "pk"
    pub f_single: bool, // "single"
}

// PortsNRNestedFindFirstN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindFirstN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedFindFirstN1 — CONCRETE native ports for node 'n1' (pluck). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindFirstN1 {
    pub f_col: String, // "col"
    pub f_rows: Vec<WireValue>, // "rows"
}

// PortsNRNestedFindFirstN2 — CONCRETE native ports for node 'n2' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindFirstN2 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedFindFirstN3 — CONCRETE native ports for node 'n3' (group). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindFirstN3 {
    pub f_children: Vec<WireValue>, // "children"
    pub f_fk: String, // "fk"
    pub f_into: String, // "into"
    pub f_parents: Vec<WireValue>, // "parents"
    pub f_pk: String, // "pk"
    pub f_single: bool, // "single"
}

// PortsNRNestedFindUniqueN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindUniqueN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedFindUniqueN1 — CONCRETE native ports for node 'n1' (pluck). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindUniqueN1 {
    pub f_col: String, // "col"
    pub f_rows: Vec<WireValue>, // "rows"
}

// PortsNRNestedFindUniqueN2 — CONCRETE native ports for node 'n2' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindUniqueN2 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedFindUniqueN3 — CONCRETE native ports for node 'n3' (group). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedFindUniqueN3 {
    pub f_children: Vec<WireValue>, // "children"
    pub f_fk: String, // "fk"
    pub f_into: String, // "into"
    pub f_parents: Vec<WireValue>, // "parents"
    pub f_pk: String, // "pk"
    pub f_single: bool, // "single"
}

// PortsNRNestedRelationsN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedRelationsN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedRelationsN1 — CONCRETE native ports for node 'n1' (pluck). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedRelationsN1 {
    pub f_col: String, // "col"
    pub f_rows: Vec<WireValue>, // "rows"
}

// PortsNRNestedRelationsN2 — CONCRETE native ports for node 'n2' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedRelationsN2 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedRelationsN3 — CONCRETE native ports for node 'n3' (pluck). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedRelationsN3 {
    pub f_col: String, // "col"
    pub f_rows: Vec<WireValue>, // "rows"
}

// PortsNRNestedRelationsN4 — CONCRETE native ports for node 'n4' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedRelationsN4 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRNestedRelationsN5 — CONCRETE native ports for node 'n5' (group). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedRelationsN5 {
    pub f_children: Vec<WireValue>, // "children"
    pub f_fk: String, // "fk"
    pub f_into: String, // "into"
    pub f_parents: Vec<WireValue>, // "parents"
    pub f_pk: String, // "pk"
    pub f_single: bool, // "single"
}

// PortsNRNestedRelationsN6 — CONCRETE native ports for node 'n6' (group). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRNestedRelationsN6 {
    pub f_children: Vec<WireValue>, // "children"
    pub f_fk: String, // "fk"
    pub f_into: String, // "into"
    pub f_parents: Vec<WireValue>, // "parents"
    pub f_pk: String, // "pk"
    pub f_single: bool, // "single"
}

// PortsNRCompositeRelationsN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRCompositeRelationsN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRCompositeRelationsN1 — CONCRETE native ports for node 'n1' (pluck). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRCompositeRelationsN1 {
    pub f_col: String, // "col"
    pub f_rows: Vec<WireValue>, // "rows"
}

// PortsNRCompositeRelationsN2 — CONCRETE native ports for node 'n2' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRCompositeRelationsN2 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRCompositeRelationsN3 — CONCRETE native ports for node 'n3' (group). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRCompositeRelationsN3 {
    pub f_children: Vec<WireValue>, // "children"
    pub f_fk: String, // "fk"
    pub f_into: String, // "into"
    pub f_parents: Vec<WireValue>, // "parents"
    pub f_pk: String, // "pk"
    pub f_single: bool, // "single"
}

// PortsNRCreateN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRCreateN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRUpdateN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRUpdateN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// PortsNRUpsertN0 — CONCRETE native ports for node 'n0' (executeSQL). Typed fields per the
// static port type; constructed directly (no Vec, no heap key strings, no per-port Value boxing). The
// handler reads the typed fields directly off this concrete struct — no by-name accessor.
#[derive(Clone)]
pub struct PortsNRUpsertN0 {
    pub f_bigint: bool, // "bigint"
    pub f_params: Vec<WireValue>, // "params"
    pub f_returning: bool, // "returning"
    pub f_sql: String, // "sql"
    pub f_write: bool, // "write"
}

// CONCRETE per-component input structs (fields = inputPorts).
// InNRFindAll — the CONCRETE input for 'findAll' (no input ports).
#[derive(Default)]
pub struct InNRFindAll;

// InNRFilterPaginateSort — the CONCRETE input for 'filterPaginateSort' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRFilterPaginateSort {
    pub published: WireValue, // "published"
}
impl Default for InNRFilterPaginateSort {
    fn default() -> Self {
        InNRFilterPaginateSort {
            published: WireValue::Null,
        }
    }
}

// InNRFindFirst — the CONCRETE input for 'findFirst' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRFindFirst {
    pub _like: Value, // "@like"
    pub name: WireValue, // "name"
}
impl Default for InNRFindFirst {
    fn default() -> Self {
        InNRFindFirst {
            _like: Value::Null,
            name: WireValue::Null,
        }
    }
}

// InNRFindUnique — the CONCRETE input for 'findUnique' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRFindUnique {
    pub email: WireValue, // "email"
}
impl Default for InNRFindUnique {
    fn default() -> Self {
        InNRFindUnique {
            email: WireValue::Null,
        }
    }
}

// InNRNestedFindAll — the CONCRETE input for 'nestedFindAll' (no input ports).
#[derive(Default)]
pub struct InNRNestedFindAll;

// InNRNestedFindFirst — the CONCRETE input for 'nestedFindFirst' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRNestedFindFirst {
    pub _like: Value, // "@like"
    pub name: WireValue, // "name"
}
impl Default for InNRNestedFindFirst {
    fn default() -> Self {
        InNRNestedFindFirst {
            _like: Value::Null,
            name: WireValue::Null,
        }
    }
}

// InNRNestedFindUnique — the CONCRETE input for 'nestedFindUnique' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRNestedFindUnique {
    pub email: WireValue, // "email"
}
impl Default for InNRNestedFindUnique {
    fn default() -> Self {
        InNRNestedFindUnique {
            email: WireValue::Null,
        }
    }
}

// InNRNestedRelations — the CONCRETE input for 'nestedRelations' (no input ports).
#[derive(Default)]
pub struct InNRNestedRelations;

// InNRCompositeRelations — the CONCRETE input for 'compositeRelations' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRCompositeRelations {
    pub tenant_id: WireValue, // "tenant_id"
}
impl Default for InNRCompositeRelations {
    fn default() -> Self {
        InNRCompositeRelations {
            tenant_id: WireValue::Null,
        }
    }
}

// InNRCreate — the CONCRETE input for 'create' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRCreate {
    pub email: WireValue, // "email"
    pub name: WireValue, // "name"
}
impl Default for InNRCreate {
    fn default() -> Self {
        InNRCreate {
            email: WireValue::Null,
            name: WireValue::Null,
        }
    }
}

// InNRUpdate — the CONCRETE input for 'update' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRUpdate {
    pub id: WireValue, // "id"
    pub name: WireValue, // "name"
}
impl Default for InNRUpdate {
    fn default() -> Self {
        InNRUpdate {
            id: WireValue::Null,
            name: WireValue::Null,
        }
    }
}

// InNRUpsert — the CONCRETE input for 'upsert' (fields = inputPorts; typed, consumer-built —
// NO generic Value slice, NO per-field boxing crosses the covered read boundary).
pub struct InNRUpsert {
    pub email: WireValue, // "email"
    pub name: WireValue, // "name"
}
impl Default for InNRUpsert {
    fn default() -> Self {
        InNRUpsert {
            email: WireValue::Null,
            name: WireValue::Null,
        }
    }
}

// Combined read runners (STRUCT-returning — the fully de-plumbed CONCRETE path).
// run_native_raw_struct_findAll — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relationSingle /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn run_native_raw_struct_findAll(
    _in_: InNRFindAll,
) -> Result<Vec<T0>, BehaviorError> {
    let cell_n0: RefCell<Vec<T0>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let ports_n0 = PortsNRFindAllN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![]; __v }, f_returning: false, f_sql: "SELECT id, email, name FROM benchmark_users ORDER BY id ASC LIMIT 100".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => T0 {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<f64>() {
                                Ok(n) => Some(n),
                                Err(_) => return Err(de_overflow("T0", "id", "opt(float)", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "id", "opt(float)", actual_wire_type, raw_value)),
                            NumProbe::Null { .. } | NumProbe::Absent => None,
                        },
                        email: match sub1.probe_string("email") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "email", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        name: match sub1.probe_string("name") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "name", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),email:opt(string),name:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),email:opt(string),name:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),email:opt(string),name:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),email:opt(string),name:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_filterPaginateSort — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relationSingle /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn run_native_raw_struct_filterPaginateSort(
    in_: InNRFilterPaginateSort,
) -> Result<Vec<T1>, BehaviorError> {
    let cell_n0: RefCell<Vec<T1>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let ports_n0 = PortsNRFilterPaginateSortN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.published.clone()]; __v }, f_returning: false, f_sql: "SELECT id, title, content, published, author_id, created_at FROM benchmark_posts WHERE published = ? ORDER BY created_at DESC LIMIT 20 OFFSET 10".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => T1 {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<f64>() {
                                Ok(n) => Some(n),
                                Err(_) => return Err(de_overflow("T1", "id", "opt(float)", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T1", "id", "opt(float)", actual_wire_type, raw_value)),
                            NumProbe::Null { .. } | NumProbe::Absent => None,
                        },
                        title: match sub1.probe_string("title") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T1", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        content: match sub1.probe_string("content") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T1", "content", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        published: match sub1.probe_number("published") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<f64>() {
                                Ok(n) => Some(n),
                                Err(_) => return Err(de_overflow("T1", "published", "opt(float)", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T1", "published", "opt(float)", actual_wire_type, raw_value)),
                            NumProbe::Null { .. } | NumProbe::Absent => None,
                        },
                        author_id: match sub1.probe_number("author_id") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<f64>() {
                                Ok(n) => Some(n),
                                Err(_) => return Err(de_overflow("T1", "author_id", "opt(float)", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T1", "author_id", "opt(float)", actual_wire_type, raw_value)),
                            NumProbe::Null { .. } | NumProbe::Absent => None,
                        },
                        created_at: match sub1.probe_string("created_at") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T1", "created_at", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),title:opt(string),content:opt(string),published:opt(float),author_id:opt(float),created_at:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),title:opt(string),content:opt(string),published:opt(float),author_id:opt(float),created_at:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string),content:opt(string),published:opt(float),author_id:opt(float),created_at:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),title:opt(string),content:opt(string),published:opt(float),author_id:opt(float),created_at:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_findFirst — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relationSingle /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn run_native_raw_struct_findFirst(
    in_: InNRFindFirst,
) -> Result<Vec<T0>, BehaviorError> {
    let cell_n0: RefCell<Vec<T0>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let ports_n0 = PortsNRFindFirstN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.name.clone()]; __v }, f_returning: false, f_sql: "SELECT id, email, name FROM benchmark_users WHERE name LIKE ? LIMIT 1".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => T0 {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<f64>() {
                                Ok(n) => Some(n),
                                Err(_) => return Err(de_overflow("T0", "id", "opt(float)", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "id", "opt(float)", actual_wire_type, raw_value)),
                            NumProbe::Null { .. } | NumProbe::Absent => None,
                        },
                        email: match sub1.probe_string("email") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "email", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        name: match sub1.probe_string("name") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "name", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),email:opt(string),name:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),email:opt(string),name:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),email:opt(string),name:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),email:opt(string),name:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_findUnique — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relationSingle /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn run_native_raw_struct_findUnique(
    in_: InNRFindUnique,
) -> Result<Vec<T0>, BehaviorError> {
    let cell_n0: RefCell<Vec<T0>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let ports_n0 = PortsNRFindUniqueN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.email.clone()]; __v }, f_returning: false, f_sql: "SELECT id, email, name FROM benchmark_users WHERE email = ? LIMIT 1".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => T0 {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<f64>() {
                                Ok(n) => Some(n),
                                Err(_) => return Err(de_overflow("T0", "id", "opt(float)", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "id", "opt(float)", actual_wire_type, raw_value)),
                            NumProbe::Null { .. } | NumProbe::Absent => None,
                        },
                        email: match sub1.probe_string("email") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "email", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        name: match sub1.probe_string("name") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("T0", "name", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),email:opt(string),name:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),email:opt(string),name:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),email:opt(string),name:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),email:opt(string),name:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_nestedFindAll — post-#164 wire-passthrough runner. Intermediate nodes (parent
// executeSQL n0, pluck n1, child executeSQL n2) hold RAW wire (`Vec<WireValue>` / `WireValue`) and are
// NEVER de-boxed; only the TERMINAL group node (n3) de-boxes INLINE into the concrete outType. The
// leaves are called with `&[WireValue]`; the driver is the ambient one.
pub fn run_native_raw_struct_nestedFindAll(
    _in_: InNRNestedFindAll,
) -> Result<Vec<T3>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<WireValue> = RefCell::new(WireValue::Null);
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<T3>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) — RAW WIRE (intermediate) ──
    let ports_n0 = PortsNRNestedFindAllN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![]; __v }, f_returning: false, f_sql: "SELECT id, email, name FROM benchmark_users LIMIT 100".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = wire_n0.into_items();
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) — RAW WIRE (intermediate) ──
    if produced_n0.get() {
        let ports_n1 = PortsNRNestedFindAllN1 { f_col: "id".to_string(), f_rows: cell_n0.borrow().clone() };
        let wire_n1 = match pluck_keys(&ports_n1.f_col, &ports_n1.f_rows) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n1", e.message), detail: e.detail }),
        };
        *cell_n1.borrow_mut() = wire_n1;
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) — RAW WIRE (intermediate) ──
    if produced_n1.get() {
        let ports_n2 = PortsNRNestedFindAllN2 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![cell_n1.borrow().clone()]; __v }, f_returning: false, f_sql: "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC".to_string(), f_write: false };
        let wire_n2 = match execute_sql(ports_n2.f_bigint, &ports_n2.f_params, ports_n2.f_returning, &ports_n2.f_sql, ports_n2.f_write) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n2", e.message), detail: e.detail }),
        };
        *cell_n2.borrow_mut() = wire_n2.into_items();
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) — TERMINAL: de-box to the concrete outType ──
    if produced_n0.get() {
        let ports_n3 = PortsNRNestedFindAllN3 { f_children: cell_n2.borrow().clone(), f_fk: "author_id".to_string(), f_into: "posts".to_string(), f_parents: cell_n0.borrow().clone(), f_pk: "id".to_string(), f_single: false };
        let wire_n3 = match group_children(&ports_n3.f_children, &ports_n3.f_fk, &ports_n3.f_into, &ports_n3.f_parents, &ports_n3.f_pk, ports_n3.f_single) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n3", e.message), detail: e.detail }),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => T3 {

                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_nestedFindFirst — post-#164 wire-passthrough runner (see nestedFindAll).
pub fn run_native_raw_struct_nestedFindFirst(
    in_: InNRNestedFindFirst,
) -> Result<Vec<T3>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<WireValue> = RefCell::new(WireValue::Null);
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<T3>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) — RAW WIRE (intermediate) ──
    let ports_n0 = PortsNRNestedFindFirstN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.name.clone()]; __v }, f_returning: false, f_sql: "SELECT id, email, name FROM benchmark_users WHERE name LIKE ? LIMIT 1".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = wire_n0.into_items();
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) — RAW WIRE (intermediate) ──
    if produced_n0.get() {
        let ports_n1 = PortsNRNestedFindFirstN1 { f_col: "id".to_string(), f_rows: cell_n0.borrow().clone() };
        let wire_n1 = match pluck_keys(&ports_n1.f_col, &ports_n1.f_rows) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n1", e.message), detail: e.detail }),
        };
        *cell_n1.borrow_mut() = wire_n1;
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) — RAW WIRE (intermediate) ──
    if produced_n1.get() {
        let ports_n2 = PortsNRNestedFindFirstN2 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![cell_n1.borrow().clone()]; __v }, f_returning: false, f_sql: "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC".to_string(), f_write: false };
        let wire_n2 = match execute_sql(ports_n2.f_bigint, &ports_n2.f_params, ports_n2.f_returning, &ports_n2.f_sql, ports_n2.f_write) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n2", e.message), detail: e.detail }),
        };
        *cell_n2.borrow_mut() = wire_n2.into_items();
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) — TERMINAL: de-box to the concrete outType ──
    if produced_n0.get() {
        let ports_n3 = PortsNRNestedFindFirstN3 { f_children: cell_n2.borrow().clone(), f_fk: "author_id".to_string(), f_into: "posts".to_string(), f_parents: cell_n0.borrow().clone(), f_pk: "id".to_string(), f_single: false };
        let wire_n3 = match group_children(&ports_n3.f_children, &ports_n3.f_fk, &ports_n3.f_into, &ports_n3.f_parents, &ports_n3.f_pk, ports_n3.f_single) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n3", e.message), detail: e.detail }),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => T3 {

                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_nestedFindUnique — post-#164 wire-passthrough runner (see nestedFindAll).
pub fn run_native_raw_struct_nestedFindUnique(
    in_: InNRNestedFindUnique,
) -> Result<Vec<T3>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<WireValue> = RefCell::new(WireValue::Null);
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<T3>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) — RAW WIRE (intermediate) ──
    let ports_n0 = PortsNRNestedFindUniqueN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.email.clone()]; __v }, f_returning: false, f_sql: "SELECT id, email, name FROM benchmark_users WHERE email = ? LIMIT 1".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = wire_n0.into_items();
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) — RAW WIRE (intermediate) ──
    if produced_n0.get() {
        let ports_n1 = PortsNRNestedFindUniqueN1 { f_col: "id".to_string(), f_rows: cell_n0.borrow().clone() };
        let wire_n1 = match pluck_keys(&ports_n1.f_col, &ports_n1.f_rows) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n1", e.message), detail: e.detail }),
        };
        *cell_n1.borrow_mut() = wire_n1;
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) — RAW WIRE (intermediate) ──
    if produced_n1.get() {
        let ports_n2 = PortsNRNestedFindUniqueN2 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![cell_n1.borrow().clone()]; __v }, f_returning: false, f_sql: "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC".to_string(), f_write: false };
        let wire_n2 = match execute_sql(ports_n2.f_bigint, &ports_n2.f_params, ports_n2.f_returning, &ports_n2.f_sql, ports_n2.f_write) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n2", e.message), detail: e.detail }),
        };
        *cell_n2.borrow_mut() = wire_n2.into_items();
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) — TERMINAL: de-box to the concrete outType ──
    if produced_n0.get() {
        let ports_n3 = PortsNRNestedFindUniqueN3 { f_children: cell_n2.borrow().clone(), f_fk: "author_id".to_string(), f_into: "posts".to_string(), f_parents: cell_n0.borrow().clone(), f_pk: "id".to_string(), f_single: false };
        let wire_n3 = match group_children(&ports_n3.f_children, &ports_n3.f_fk, &ports_n3.f_into, &ports_n3.f_parents, &ports_n3.f_pk, ports_n3.f_single) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n3", e.message), detail: e.detail }),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => T3 {

                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_nestedRelations — post-#164 wire-passthrough runner. Two grouping levels:
// n5 (group posts←comments) is an INTERMEDIATE group held as RAW wire and fed to n6; only n6 (group
// users←posts) is TERMINAL and de-boxes. Three executeSQL nodes (n0/n2/n4) ⇒ 3 queries (N+1-free).
pub fn run_native_raw_struct_nestedRelations(
    _in_: InNRNestedRelations,
) -> Result<Vec<T3>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<WireValue> = RefCell::new(WireValue::Null);
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<WireValue> = RefCell::new(WireValue::Null);
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    let cell_n4: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n4 = std::cell::Cell::new(false);
    let _ = &produced_n4;
    let cell_n5: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n5 = std::cell::Cell::new(false);
    let _ = &produced_n5;
    let cell_n6: RefCell<Vec<T3>> = RefCell::new(Default::default());
    let produced_n6 = std::cell::Cell::new(false);
    let _ = &produced_n6;
    // ── op 'n0' (executeSQL) — RAW WIRE (intermediate) ──
    let ports_n0 = PortsNRNestedRelationsN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![]; __v }, f_returning: false, f_sql: "SELECT id, email, name FROM benchmark_users LIMIT 100".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = wire_n0.into_items();
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) — RAW WIRE (intermediate) ──
    if produced_n0.get() {
        let ports_n1 = PortsNRNestedRelationsN1 { f_col: "id".to_string(), f_rows: cell_n0.borrow().clone() };
        let wire_n1 = match pluck_keys(&ports_n1.f_col, &ports_n1.f_rows) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n1", e.message), detail: e.detail }),
        };
        *cell_n1.borrow_mut() = wire_n1;
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) — RAW WIRE (intermediate) ──
    if produced_n1.get() {
        let ports_n2 = PortsNRNestedRelationsN2 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![cell_n1.borrow().clone()]; __v }, f_returning: false, f_sql: "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC".to_string(), f_write: false };
        let wire_n2 = match execute_sql(ports_n2.f_bigint, &ports_n2.f_params, ports_n2.f_returning, &ports_n2.f_sql, ports_n2.f_write) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n2", e.message), detail: e.detail }),
        };
        *cell_n2.borrow_mut() = wire_n2.into_items();
        produced_n2.set(true);
    }
    // ── op 'n3' (pluck, parent:n2) — RAW WIRE (intermediate) ──
    if produced_n2.get() {
        let ports_n3 = PortsNRNestedRelationsN3 { f_col: "id".to_string(), f_rows: cell_n2.borrow().clone() };
        let wire_n3 = match pluck_keys(&ports_n3.f_col, &ports_n3.f_rows) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n3", e.message), detail: e.detail }),
        };
        *cell_n3.borrow_mut() = wire_n3;
        produced_n3.set(true);
    }
    // ── op 'n4' (executeSQL, parent:n3) — RAW WIRE (intermediate) ──
    if produced_n3.get() {
        let ports_n4 = PortsNRNestedRelationsN4 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![cell_n3.borrow().clone()]; __v }, f_returning: false, f_sql: "SELECT id, body, post_id FROM benchmark_comments WHERE post_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC".to_string(), f_write: false };
        let wire_n4 = match execute_sql(ports_n4.f_bigint, &ports_n4.f_params, ports_n4.f_returning, &ports_n4.f_sql, ports_n4.f_write) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n4", e.message), detail: e.detail }),
        };
        *cell_n4.borrow_mut() = wire_n4.into_items();
        produced_n4.set(true);
    }
    // ── op 'n5' (group, parent:n2) — INTERMEDIATE group: held as RAW wire, fed to n6 ──
    if produced_n2.get() {
        let ports_n5 = PortsNRNestedRelationsN5 { f_children: cell_n4.borrow().clone(), f_fk: "post_id".to_string(), f_into: "comments".to_string(), f_parents: cell_n2.borrow().clone(), f_pk: "id".to_string(), f_single: false };
        let wire_n5 = match group_children(&ports_n5.f_children, &ports_n5.f_fk, &ports_n5.f_into, &ports_n5.f_parents, &ports_n5.f_pk, ports_n5.f_single) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n5", e.message), detail: e.detail }),
        };
        *cell_n5.borrow_mut() = wire_n5.into_items();
        produced_n5.set(true);
    }
    // ── op 'n6' (group, parent:n0) — TERMINAL: de-box to the concrete outType ──
    if produced_n0.get() {
        let ports_n6 = PortsNRNestedRelationsN6 { f_children: cell_n5.borrow().clone(), f_fk: "author_id".to_string(), f_into: "posts".to_string(), f_parents: cell_n0.borrow().clone(), f_pk: "id".to_string(), f_single: false };
        let wire_n6 = match group_children(&ports_n6.f_children, &ports_n6.f_fk, &ports_n6.f_into, &ports_n6.f_parents, &ports_n6.f_pk, ports_n6.f_single) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n6", e.message), detail: e.detail }),
        };
        *cell_n6.borrow_mut() = match wire_n6.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => T3 {

                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n6", "n6", "obj{}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n6", "n6", "obj{}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n6", "n6", "arr(obj{})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n6", "n6", "arr(obj{})")),
        };
        produced_n6.set(true);
    }
    let __out = cell_n6.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_compositeRelations — post-#164 wire-passthrough runner (see nestedFindAll).
pub fn run_native_raw_struct_compositeRelations(
    in_: InNRCompositeRelations,
) -> Result<Vec<T3>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<WireValue> = RefCell::new(WireValue::Null);
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<T3>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) — RAW WIRE (intermediate) ──
    let ports_n0 = PortsNRCompositeRelationsN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.tenant_id.clone()]; __v }, f_returning: false, f_sql: "SELECT tenant_id, user_id, name FROM benchmark_tenant_users WHERE tenant_id = ? ORDER BY user_id ASC".to_string(), f_write: false };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = wire_n0.into_items();
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) — RAW WIRE (intermediate; composite-key col is emitted empty by bc) ──
    if produced_n0.get() {
        let ports_n1 = PortsNRCompositeRelationsN1 { f_col: "".to_string(), f_rows: cell_n0.borrow().clone() };
        let wire_n1 = match pluck_keys(&ports_n1.f_col, &ports_n1.f_rows) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n1", e.message), detail: e.detail }),
        };
        *cell_n1.borrow_mut() = wire_n1;
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) — RAW WIRE (intermediate) ──
    if produced_n1.get() {
        let ports_n2 = PortsNRCompositeRelationsN2 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![cell_n1.borrow().clone()]; __v }, f_returning: false, f_sql: "SELECT tenant_id, post_id, user_id, title FROM benchmark_tenant_posts WHERE EXISTS (SELECT 1 FROM json_each(?) je WHERE json_extract(je.value, '$[0]') = benchmark_tenant_posts.tenant_id AND json_extract(je.value, '$[1]') = benchmark_tenant_posts.user_id) ORDER BY post_id ASC".to_string(), f_write: false };
        let wire_n2 = match execute_sql(ports_n2.f_bigint, &ports_n2.f_params, ports_n2.f_returning, &ports_n2.f_sql, ports_n2.f_write) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n2", e.message), detail: e.detail }),
        };
        *cell_n2.borrow_mut() = wire_n2.into_items();
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) — TERMINAL: de-box to the concrete outType ──
    if produced_n0.get() {
        let ports_n3 = PortsNRCompositeRelationsN3 { f_children: cell_n2.borrow().clone(), f_fk: "".to_string(), f_into: "posts".to_string(), f_parents: cell_n0.borrow().clone(), f_pk: "".to_string(), f_single: false };
        let wire_n3 = match group_children(&ports_n3.f_children, &ports_n3.f_fk, &ports_n3.f_into, &ports_n3.f_parents, &ports_n3.f_pk, ports_n3.f_single) {
            Ok(r) => r,
            Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n3", e.message), detail: e.detail }),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => T3 {

                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.borrow().clone();
    Ok(__out)
}


// run_native_raw_struct_create — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relationSingle /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn run_native_raw_struct_create(
    in_: InNRCreate,
) -> Result<Vec<T3>, BehaviorError> {
    let cell_n0: RefCell<Vec<T3>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let ports_n0 = PortsNRCreateN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.email.clone(), in_.name.clone()]; __v }, f_returning: false, f_sql: "INSERT INTO benchmark_users (email, name) VALUES (?, ?)".to_string(), f_write: true };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => T3 {

                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_update — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relationSingle /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn run_native_raw_struct_update(
    in_: InNRUpdate,
) -> Result<Vec<T3>, BehaviorError> {
    let cell_n0: RefCell<Vec<T3>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let ports_n0 = PortsNRUpdateN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.name.clone(), in_.id.clone()]; __v }, f_returning: false, f_sql: "UPDATE benchmark_users SET name = ? WHERE id = ?".to_string(), f_write: true };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => T3 {

                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// run_native_raw_struct_upsert — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relationSingle /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn run_native_raw_struct_upsert(
    in_: InNRUpsert,
) -> Result<Vec<T3>, BehaviorError> {
    let cell_n0: RefCell<Vec<T3>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let ports_n0 = PortsNRUpsertN0 { f_bigint: false, f_params: { let __v: Vec<WireValue> = vec![in_.email.clone(), in_.name.clone()]; __v }, f_returning: true, f_sql: "INSERT INTO benchmark_users (email, name) VALUES (?, ?) ON CONFLICT (email) DO UPDATE SET email = excluded.email, name = excluded.name RETURNING id".to_string(), f_write: true };
    let wire_n0 = match execute_sql(ports_n0.f_bigint, &ports_n0.f_params, ports_n0.f_returning, &ports_n0.f_sql, ports_n0.f_write) {
        Ok(r) => r,
        Err(e) => return Err(BehaviorError { code: "OP_FAILED".to_string(), message: format!("operation '{}' failed under 'fail' policy: {}", "n0", e.message), detail: e.detail }),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => T3 {

                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// COMPONENT_NAMES_NATIVE_RAW — covered reads exposed on the combined struct-native path. Each is
// driven via run_native_raw_struct_<comp>(in_) -> Result<T, BehaviorError>: a STRUCT return (the consumer
// builds the CONCRETE InNR_<comp> input + supplies the op-agnostic leaf transport symbols the runner calls).
// See INTEGRATION.md §6.
pub const COMPONENT_NAMES_NATIVE_RAW: [&str; 12] = ["findAll", "filterPaginateSort", "findFirst", "findUnique", "nestedFindAll", "nestedFindFirst", "nestedFindUnique", "nestedRelations", "compositeRelations", "create", "update", "upsert"];
