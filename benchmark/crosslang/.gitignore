# Transient run output
.crosslang-results.json
# v1 baseline install (npm alias) — reinstalled via v1ts/package.json
v1ts/node_modules/
# Compiled adapter binaries + build dirs
adapters/rust/target/
adapters/rust-codegen/target/
adapters/go/go_bench
adapters/go-codegen/lm_codegen
adapters/v1rs/target/
# Python bytecode
adapters/python/__pycache__/
**/__pycache__/

# Generated build output — regenerated at run time (NEVER commit): the old #44 bundles.json +
# codegen/{ts,py,go,rust,php} modules. Ignore everything under generated/ EXCEPT the committed,
# drift-checked #63 orm-plan.json artifact (the language-neutral SSoT every runtime consumes).
generated/*
!generated/orm-plan.json
